site_name: 'github-app-token-generator'
site_description: 'GitHub Action that can be used to generate an installation access token for a GitHub App.'
#uncomment the below lines if you want to enable the edit button in your techdocs
#edit_uri: blob/main/docs/ #don't change it, just uncomment
#repo_url: '<insert your repo url here>'
theme:
  name: material
  palette:
    scheme: default
  features:
    - toc.follow
    - toc.integrate
plugins:
  - techdocs-core
